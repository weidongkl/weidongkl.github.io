# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概览

个人博客/笔记站点（weidongkl.github.io），基于 Docusaurus 3.8 + preset-classic，默认语言 zh-Hans，支持英文 i18n。内容以中文技术笔记为主，无测试/lint 配置，Node >= 18（CI 使用 Node 18）。

## 常用命令

| 命令 | 用途 |
|------|------|
| `npm start` | 本地开发服务器（热更新） |
| `npm run build` | 构建到 `build/`（`onBrokenLinks: warn`，断链只警告不失败） |
| `npm run serve` | 本地预览构建产物 |
| `npm run clear` | 清理 `.docusaurus` 缓存，构建异常时先执行 |
| `npm run generate-recent` | 根据 `docs/` 的 git 历史重新生成 `src/data/recentDocs.json`（最近 25 篇变更文档，供首页"最近更新"使用） |
| `npm run write-translations` | 提取/更新主题翻译文件到 `i18n/` |

其他 Docusaurus CLI 子命令（如 `docs:version`）通过 `npx docusaurus <cmd>` 执行。根目录 `cli.sh` 仅简单封装了 start/deploy，基本不使用（且其中 deploy 有拼写错误）。

## 部署

- **无需手动部署**：push 到 `master` 触发 `.github/workflows/deploy.yml`，CI 构建后由 peaceiris/actions-gh-pages 将 `build/` 发布到 `docs-deploy` 分支（GitHub Pages 源设置为该分支）。
- 另一个 workflow `cleanup-workflows.yml` 每周清理 30 天前的旧 workflow runs，与站点无关。

## 内容架构

- `docs/note/` — 笔记主体。`sidebars.js` 中唯一生效的 sidebar 是 `note`（`autogenerated`，按目录结构自动生成，**新增 markdown 文件即自动进入侧边栏，无需改 sidebars.js**）。子目录用 `_category_.json` 控制分类显示（label/position/link），如 `docs/note/golang/design/_category_.json` 配置了 `generated-index`，使首页"查看全部笔记"链接 `/docs/category/design` 生效。
- `blog/` — 博客文章，文件名必须为 `YYYY-MM-DD-标题.md`；作者/标签在 `authors.yml`、`tags.yml` 维护。
- `src/pages/` — 独立页面（`index.js` 首页、`about.js` 等），首页实际内容由 `src/components/HomepageFeatures` 渲染。
- `src/theme/Layout.js` — 通过主题包装（swizzle 模式）向全站注入 `src/components/FixedBanner`（固定横幅组件）。修改全站布局相关逻辑时看这里。
- `src/data/recentDocs.json` — 由 `scripts/generateRecentDocs.js` 生成，**不要手改**；文档增删后运行 `npm run generate-recent` 更新。

## i18n

- 默认语言 zh-Hans，locales 为 `['en', 'zh-Hans']`。中文原文直接写在 `docs/`、`blog/`、`src/pages/` 下；英文内容放在 `i18n/en/docusaurus-plugin-content-*`（博客为整篇翻译拷贝，文档为 `current.json`，页面为组件拷贝）。
- 组件内需要翻译的文案用 `<Translate id="...">` 包裹（见 `HomepageFeatures`），配合 `write-translations` 维护 `i18n/en/code.json`。
- `docusaurus.config.js` 的 `customFields.watchOptions` 将 `i18n/**/docusaurus-plugin-content-*` 排除在文件监视之外：修改英文翻译后开发服务器不会热更新，需重启 `npm start`。

## 注意事项

- `docs/note/` 下的 `intro.md_bak`、`sidebars.js.bak`、`src/components/HomepageFeatures_bak/` 是历史备份文件，不要修改或删除。
- 根目录 `navbar.json` 未被任何代码引用，是遗留文件。
- 站点启用了 `@easyops-cn/docusaurus-search-local`（中英文本地搜索，hashed 索引）和 `@docusaurus/theme-mermaid`（markdown 中可直接写 mermaid 代码块）。
- 主题固定 light 模式（`disableSwitch: true`），不做暗色适配。
